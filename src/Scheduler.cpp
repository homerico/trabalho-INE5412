
#include <iostream>
#include "Scheduler.h"
#include <utility>

Process *Scheduler::schedule(std::vector<Process *> &waitingProcesses, Process *runningProcess) {
    waitingProcesses.size(); // Operação para suprimir warning
    return runningProcess;
}
